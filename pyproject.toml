[project]
name = "portfolio-homelab-k8s"
version = "0.1.0"
description = "Home Lab Kubernetes Infrastructure Portfolio"
authors = [
    {name = "Your Name", email = "your.email@example.com"}
]
readme = "README.md"
requires-python = ">=3.9"

dependencies = [
    "pre-commit>=4.2.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
dev-dependencies = [
    "pre-commit>=4.2.0",
]
